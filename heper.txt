python -m venv .venv
.venv\Scripts\activate
pip list
pip install --upgrade pip
pip install -r requirements.txt
playwright install

behave -f allure_behave.formatter:AllureFormatter -o allure-results

Reporte allure
1- Instalar scoop en windows
iwr -useb get.scoop.sh | iex
2- scoop --version
3- Instalar allure con scoop
scoop install allure
4- allure --version


//// Para una ejecucion
behave -f allure_behave.formatter:AllureFormatter -o allure-results
allure generate allure-results -o allure-report --clean --single-file
allure serve allure-results

--------------------------------



//// Para una ejecucion con runner.py
behave -f allure_behave.formatter:AllureFormatter -o allure-results
allure generate reports/login -o allure-report --clean --single-file
allure serve allure-results


## aca 1
allure generate reports/login -o allure-report --clean
## aca 2
allure generate allure-results -o allure-report --clean
allure serve allure-results










behave -f allure_behave.formatter:AllureFormatter -o reports/ features
allure generate reports/ -o allure-html --clean --single-file





---------
Construye la imagen definida en tu Dockerfile.
docker-compose build
Pero usando la configuración de docker-compose.yml.

🔍 ¿Cuándo se usa?

Cuando modificás el Dockerfile.

Cuando agregás dependencias nuevas.

Cuando querés regenerar la imagen desde cero.

🧠 Importante

docker-compose build solo construye.
No ejecuta el contenedor.



🚀 ¿Qué hace docker-compose up --build?
👉 Hace dos cosas juntas:
Construye la imagen (si cambió o si ponés --build obligás a reconstruirla).
Levanta y ejecuta el contenedor.
Es decir, reemplaza a:

docker-compose up --build



Limpiar todas las capas corruptas
docker system prune -af
docker builder prune -af

Reset completo de Docker
docker system prune -a --volumes

Esto elimina:
caché roto
capas incompletas
builds fallidos

---------------------------
eliminar todo los datos
docker system prune -af
docker builder prune -af



docker-compose up --build
obtener Reporte

docker cp python_playwright_allure:/app/allure-report C:\Users\PC\Desktop\testin